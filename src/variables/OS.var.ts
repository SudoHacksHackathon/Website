const OSObject: {
  OS: string;
  commandStart: string;
  commands: { print: string; execute: string };
} = {
  OS: "",
  commandStart: "",
  commands: { print: "", execute: "" },
};

export default OSObject;
